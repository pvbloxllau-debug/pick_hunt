// Chart.js stub - CDN no disponible en red Walmart.
// El dashboard principal funciona 100%. Los graficos de /stats estan deshabilitados.
(function(){
  var noop = function(){};
  var noopObj = { destroy:noop, update:noop, data:{datasets:[]}, options:{} };
  function Chart(ctx, cfg) {
    console.warn('[Picker Hunt] Chart.js no disponible - graficos deshabilitados');
    return noopObj;
  }
  Chart.register = noop;
  Chart.defaults = { color:'#666', font:{} };
  window.Chart = Chart;
})();
